# Temporary Yatsukusa Park artifact build

This branch is used only as a reproducible Blender build runner for the user-requested 八ツ草公園 VRChat model. It does not modify `main`; the pull request will be closed after the workflow artifact is retrieved.
